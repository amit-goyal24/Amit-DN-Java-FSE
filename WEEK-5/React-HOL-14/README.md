# React HOL 14

This folder contains a minimal React project scaffold for the hands-on document inside this directory.

## Run locally

1. Open this folder in a terminal.
2. Run `npm install`.
3. Run `npm start`.
